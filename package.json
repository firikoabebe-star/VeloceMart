{
  "name": "velocemart",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev & cd backend && npm run start:dev",
    "build": "cd frontend && npm run build && cd ../backend && npm run build",
    "lint": "cd frontend && npm run lint && cd ../backend && npm run lint"
  }
}
